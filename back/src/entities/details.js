export class Detail {
  constructor(id, idOrder, idProduct, quantity, totalPrice) {
    this.id = id
    this.id_order = idOrder
    this.idProduct = idProduct
    this.quantity = quantity
    this.totalPrice = totalPrice
  }
}
